{
  "name": "native-example-plugin",
  "version": "1.0.0",
  "description": "原生插件示例",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:rust": "cargo build",
    "build:rust:release": "cargo build --release",
    "build:vue": "vite build",
    "package": "node build.js --release --package",
    "dev": "cargo watch -x build",
    "clean": "cargo clean && rimraf dist dist-ui"
  },
  "keywords": [
    "aio-hub",
    "plugin",
    "native"
  ],
  "author": "咕咕",
  "license": "MIT",
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "archiver": "^7.0.1",
    "fs-extra": "^11.2.0",
    "rimraf": "^5.0.7",
    "vite": "^5.0.0"
  }
}